import { RangefilterPipe } from './rangefilter.pipe';

describe('RangefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RangefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
